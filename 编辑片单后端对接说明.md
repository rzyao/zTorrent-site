# 编辑片单后端对接说明


## 页面功能概述
- 片单基本操作：创建、编辑、删除、查看详情、列表分页与搜索。
- 片单影片管理：在片单内添加影片、移除影片、排序（拖拽后提交新顺序）。
- 展示与统计：封面、标题、描述、可见性、影片数量、创建时间、浏览次数、点赞数。
- 影片选择面板：分页搜索影片库，选择后添加到片单。

## 请求约定
- 统一使用 HTTP `POST`。
- 统一使用 `Content-Type: application/json`，参数在请求 body。
- 不做旧兼容：新增/变更以本文字段为准，旧字段或格式不再兼容。

## 数据模型（建议统一 DTO）
- PlaylistDTO（片单详情）
  - `id: string`
  - `name: string`
  - `description?: string`
  - `visibility: 'public'|'private'|'friends'`
  - `coverUrl?: string`
  - `tags?: string[]`
  - `films: PlaylistItemDTO[]`
  - `stats: { views: number; likes: number }`
  - `meta: { createdAt: string; updatedAt: string; ownerId?: string }`
- PlaylistItemDTO（片单内的影片项，供排序和展示）
  - `filmId: string`
  - `title: string`
  - `poster?: string`
  - `year?: number`
  - `duration?: number`
  - `position: number`
- PlaylistSummaryDTO（片单列表项）
  - `id: string`
  - `name: string`
  - `visibility: 'public'|'private'|'friends'`
  - `coverUrl?: string`
  - `filmCount: number`
  - `stats: { views: number; likes: number }`
  - `meta: { createdAt: string; updatedAt: string }`
- FilmListItemDTO（影片库列表用于添加面板）
  - `id: string`
  - `title: string`
  - `poster?: string`
  - `year?: number`
  - `duration?: number`
  - `genres?: string[]`

## 接口规格（POST + JSON body）

### 创建片单
- 路径：`/playlists/create-playlist`
- 请求：
  - `name: string`
  - `description?: string`
  - `visibility: 'public'|'private'|'friends'`
  - `coverUrl?: string`
  - `tags?: string[]`
- 响应：`{ code: 0, message: 'ok', data: PlaylistDTO }`
- 原因：前端保存时需一次性落库全部基础字段；目前部分接口缺少 `tags/coverUrl`，需补全。

### 更新片单
- 路径：`/playlists/update-playlist`
- 请求：
  - `id: string`
  - `name?: string`
  - `description?: string`
  - `visibility?: 'public'|'private'|'friends'`
  - `coverUrl?: string`
  - `tags?: string[]`
- 响应：`{ code: 0, message: 'ok', data: PlaylistDTO }`
- 原因：编辑页需要更新任意字段并拿到最新详情回填；需支持 `coverUrl/tags/visibility` 的更新。

### 删除片单
- 路径：`/playlists/delete-playlist`
- 请求：`{ id: string }`
- 响应：`{ code: 0, message: 'ok', data: { deleted: true } }`
- 原因：列表页与编辑页删除后需明确结果；简化为布尔标志。

### 获取片单详情
- 路径：`/playlists/get-playlist`
- 请求：`{ id: string }`
- 响应：`{ code: 0, message: 'ok', data: PlaylistDTO }`
- 原因：编辑页右侧详情与影片面板需完整片单数据；需确保 `films` 包含 `position/title/poster`。

### 列出片单
- 路径：`/playlists/list-playlists`
- 请求：
  - `page: number`
  - `limit: number`
  - `keyword?: string`
  - `visibility?: 'public'|'private'|'friends'`
- 响应：`{ code: 0, message: 'ok', data: { items: PlaylistSummaryDTO[], total: number } }`
- 原因：支持分页与搜索，前端展示统计与封面缩略图；需补全 `stats/filmCount/coverUrl`。

### 片单添加影片
- 路径：`/playlists/add-film`
- 请求：`{ playlistId: string, filmId: string }`
- 响应：`{ code: 0, message: 'ok', data: PlaylistDTO }`
- 原因：添加后需要最新片单（含影片项的 `position/title/poster`）；避免返回仅布尔值导致再次拉详情。

### 片单移除影片
- 路径：`/playlists/remove-film`
- 请求：`{ playlistId: string, filmId: string }`
- 响应：`{ code: 0, message: 'ok', data: PlaylistDTO }`
- 原因：移除后需最新片单以刷新编辑视图与影片计数。

### 片单影片排序
- 路径：`/playlists/reorder-film`
- 请求：`{ playlistId: string, order: string[] }`  // `order` 为片单内影片的 `filmId` 新顺序
- 响应：`{ code: 0, message: 'ok', data: { films: PlaylistItemDTO[] } }` 或返回完整 `PlaylistDTO`
- 原因：拖拽排序后需拿到更新后的 `position` 列表；前端可直接替换渲染。

###（可选）点赞与浏览递增
- 路径：`/playlists/like`、`/playlists/inc-views`
- 请求：`{ id: string }`
- 响应：`{ code: 0, message: 'ok', data: { likes?: number, views?: number } }`
- 原因：编辑页不强依赖，但列表/详情页已使用；建议保持字段一致性。

### 封面上传（新增或统一化）
- 路径：`/images/upload`
- 请求：
  - `content: string`  // base64 编码图片内容
  - `filename: string`
  - `mimeType: string`
- 响应：`{ code: 0, message: 'ok', data: { url: string } }`
- 原因：编辑页存在“上传封面”按钮；若后端不支持 `multipart/form-data`，采用 JSON base64 上传以满足“POST body”约束。

### 影片库列表（用于添加面板）
- 路径：`/films/list-films`
- 请求：
  - `page: number`
  - `limit: number`
  - `keyword?: string`
  - `genreIds?: string[]`
- 响应：`{ code: 0, message: 'ok', data: { items: FilmListItemDTO[], total: number } }`
- 原因：替换当前面板的静态 `availableMovies`；提供分页与搜索能力，字段需覆盖标题/海报/年份/时长。

## 缺失/需补全字段清单
- PlaylistsService 相关响应目前多为 `Record<string, any>`：
  - `create/update/get/list`：需返回严格的 `PlaylistDTO/PlaylistSummaryDTO`，补全 `coverUrl/tags/stats/meta/filmCount/films.position/title/poster`。
  - `add-film/remove-film/reorder-film`：返回最新片单或至少 `films: PlaylistItemDTO[]`；避免布尔值响应导致额外一次详情查询。
- FilmsService：
  - `list-films`：列表项需补全 `poster/year/duration/genres`，与添加面板展示一致。
- Upload/ImagesService：
  - 若无 `images/upload`，需新增并返回稳定的 `url` 字段；若已有上传接口但依赖 `multipart/form-data`，需新增 JSON base64 版本以满足统一约定。

## 错误码与响应规范
- 成功：`{ code: 0, message: 'ok', data: ... }`
- 失败：
  - 参数错误：`{ code: 1001, message: 'invalid_params' }`
  - 未找到：`{ code: 1004, message: 'not_found' }`
  - 权限不足：`{ code: 1403, message: 'forbidden' }`
- 服务器错误：`{ code: 1500, message: 'internal_error' }`
- 原因：前端统一处理“非 0 即失败”，并在编辑页提示错误；错误码便于埋点和问题追踪。

## 兼容性声明
- 不做旧兼容：
  - 所有接口改造按本文字段与响应格式输出。
  - 旧的 query 参数或部分响应字段（如非规范的 `any`）不再支持。

## 备注与落地建议
- 添加面板数据源需切换为后端 `list-films`，避免使用静态 `availableMovies`。
- 排序交互建议接入拖拽库，提交 `order: string[]` 至 `reorder-film`。
- 统一在保存/添加/移除/排序成功后返回最新片单详情，减少二次拉取。

