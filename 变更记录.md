# 变更记录（路由重命名简表）

- 旧: `POST /forum/threads/get-thread` → 新: `POST /forum/threads/detail`
- 旧: `POST /forum/threads/list-threads` → 新: `POST /forum/threads/list`
- 旧: `POST /forum/categories/list-categories` → 新: `POST /forum/categories/list`
- 旧: `POST /forum/posts/list-posts` → 新: `POST /forum/posts/list`
- 旧: `POST /forum/stickies/list-stickies` → 新: `POST /forum/stickies/list`
- 旧: `POST /forum/promotions/list-promotions` → 新: `POST /forum/promotions/list`

- 旧: `POST /settings/list-setting` → 新: `POST /settings/list`
- 旧: `POST /settings/list-setting-by-group` → 新: `POST /settings/groups/list`
- 旧: `POST /settings/update-items` → 新: `POST /settings/items/update`
- 旧: `POST /settings/create-setting` → 新: `POST /settings/create`
- 旧: `POST /settings/delete-setting` → 新: `POST /settings/delete`
- 旧: `POST /settings/update-setting-meta` → 新: `POST /settings/meta/update`

- 旧: `POST /users/list-users` → 新: `POST /users/list`

- 旧: `POST /levels/list-levels` → 新: `POST /levels/list`
- 旧: `POST /levels/list-permissions` → 新: `POST /levels/permissions/list`

- 旧: `POST /roles/list-roles` → 新: `POST /roles/list`

- 旧: `POST /punishments/apply-punishment` → 新: `POST /punishments/apply`
- 旧: `POST /punishments/revoke-punishment` → 新: `POST /punishments/revoke`
- 旧: `POST /punishments/list-active-by-user` → 新: `POST /punishments/user/active/list`
- 旧: `POST /punishments/list-all-by-user` → 新: `POST /punishments/user/all/list`
- 旧: `POST /punishments/list-records` → 新: `POST /punishments/records/list`

- 旧: `POST /promotions/list-promotions` → 新: `POST /promotions/list`
- 旧: `POST /promotions/torrents/list-promotions` → 新: `POST /promotions/torrents/records/list`

- 旧: `POST /playlists/create-playlist` → 新: `POST /playlists/create`
- 旧: `POST /playlists/update-playlist` → 新: `POST /playlists/update`
- 旧: `POST /playlists/delete-playlist` → 新: `POST /playlists/delete`
- 旧: `POST /playlists/list-playlists` → 新: `POST /playlists/list`
- 旧: `POST /playlists/get-playlist` → 新: `POST /playlists/detail`
- 旧: `POST /playlists/admin/list-playlists` → 新: `POST /playlists/admin/list`
- 旧: `POST /playlists/list-items` → 新: `POST /playlists/items/list`
- 旧: `POST /playlists/search-addable-items` → 新: `POST /playlists/items/search-addable`
- 旧: `POST /playlists/list-categories` → 新: `POST /playlists/categories/list`

- 旧: `POST /permissions/list-permissions` → 新: `POST /permissions/list`
- 旧: `POST /permissions/list-permissions-tree` → 新: `POST /permissions/list/all`

- 旧: `POST /unban-reasons/list-unban-reasons` → 新: `POST /unban-reasons/list`
- 旧: `POST /punishment-types/list-punishment-types` → 新: `POST /punishment-types/list`
- 旧: `POST /ban-reasons/list-ban-reasons` → 新: `POST /ban-reasons/list`
- 旧: `POST /ban-days/list-ban-days` → 新: `POST /ban-days/list`

- 旧: `POST /categories/admin/list-categories` → 新: `POST /categories/admin/list`

- 旧: `POST /bonus/admin/list-balances` → 新: `POST /bonus/admin/balances/list`
- 旧: `POST /bonus/admin/list-ledger` → 新: `POST /bonus/admin/ledger/list`
- 旧: `POST /bonus/admin/get-rules` → 新: `POST /bonus/admin/rules/get`
- 旧: `POST /bonus/admin/config-rules` → 新: `POST /bonus/admin/rules/update`
- 旧: `POST /bonus/admin/export-ledger` → 新: `POST /bonus/admin/ledger/export`
- 旧: `POST /movies/bind-torrents` → 新: `POST /movies/torrents/bind`
- 旧: `POST /movies/unbind-torrents` → 新: `POST /movies/torrents/unbind`
- 旧: `POST /movies/list-torrents` → 新: `POST /movies/torrents/list`
-
- 旧: `POST /torrent-record/torrent-records` → 新: `POST /torrent-record/records/list`
- 旧: `POST /torrent-record/downloading-toorrent` → 新: `POST /torrent-record/torrent/downloading/list`
- 旧: `POST /torrent-record/published-toorrent` → 新: `POST /torrent-record/torrent/published/list`
- 旧: `POST /torrent-record/seeding-toorrent` → 新: `POST /torrent-record/torrent/seeding/list`
- 旧: `POST /torrent-record/completed-toorrent` → 新: `POST /torrent-record/torrent/completed/list`
- 旧: `POST /torrent-record/incomplete-toorrent` → 新: `POST /torrent-record/torrent/incomplete/list`
- 旧: `POST /torrent-record/user_records` → 新: `POST /torrent-record/user/records/list`
-
- 旧: `POST /rss/token` → 新: `POST /rss/token/detail`
- 旧: `POST /api/songs/upload-audio` → 新: `POST /music/songs/audio/upload`
- 旧: `POST /api/songs/upload-cover` → 新: `POST /music/songs/cover/upload`
-
- 旧: `POST /users/preferences/get` → 新: `POST /users/preferences/detail`
- 旧: `POST /users/preferences/save` → 新: `POST /users/preferences/update`
- 旧: `POST /users/preferences/list-categories` → 新: `POST /users/preferences/categories/list`
-
- 旧: `POST /users/privacy/get` → 新: `POST /users/privacy/detail`
- 旧: `POST /users/privacy/save` → 新: `POST /users/privacy/update`
-
- 旧: `POST /users/notifications/get` → 新: `POST /users/notifications/detail`
- 旧: `POST /users/notifications/save` → 新: `POST /users/notifications/update`
-
- 旧: `POST /recommendations/create-config` → 新: `POST /recommendations/configs/create`
- 旧: `POST /recommendations/update-config` → 新: `POST /recommendations/configs/update`
- 旧: `POST /recommendations/delete-config` → 新: `POST /recommendations/configs/delete`
- 旧: `POST /recommendations/list-ops-configs` → 新: `POST /recommendations/configs/admin/list`
- 旧: `POST /recommendations/index` → 新: `POST /recommendations/index/get`
- 旧: `POST /recommendations/content` → 新: `POST /recommendations/content/get`
- 旧: `POST /recommendations/create-tab` → 新: `POST /recommendations/tabs/create`
- 旧: `POST /recommendations/update-tab` → 新: `POST /recommendations/tabs/update`
- 旧: `POST /recommendations/delete-tab` → 新: `POST /recommendations/tabs/delete`
- 旧: `POST /recommendations/list-tabs` → 新: `POST /recommendations/tabs/admin/list`
- 旧: `POST /recommendations/active-tabs` → 新: `POST /recommendations/tabs/active/list`
-
- 旧: `POST /downloaders/categories` → 新: `POST /downloaders/categories/list`
- 旧: `POST /downloaders/tags` → 新: `POST /downloaders/tags/list`
- 旧: `POST /downloaders/paths` → 新: `POST /downloaders/paths/list`
-
- 旧: `POST /announcements/mark-read` → 新: `POST /announcements/read/mark`
-
- 旧: `POST /api/torrents/options` → 新: `POST /api/torrents/options/list`
 
- 旧: `POST /api/tickets/confirm-resolved` → 新: `POST /tickets/resolved/confirm`
- 旧: `POST /api/tickets/mark-resolved` → 新: `POST /tickets/resolved/mark`
- 旧: `POST /api/tickets/todos` → 新: `POST /tickets/todos/list`
-
- 旧: `POST /api/playlists/list-public` → 新: `POST /music/playlists/public/list`
- 旧: `POST /api/playlists/add-song` → 新: `POST /music/playlists/songs/add`
- 旧: `POST /api/playlists/remove-song` → 新: `POST /music/playlists/songs/remove`
-
- 旧: `POST /messages/mark-read` → 新: `POST /messages/read/mark`
- 旧: `POST /messages/threads/mark-read` → 新: `POST /messages/threads/read/mark`
-
- 旧: `POST /stickies/list-stickies` → 新: `POST /stickies/list`
- 旧: `POST /stickies/torrents/list-stickies` → 新: `POST /stickies/torrents/list`
- 旧: `POST /messages/notifications/mark-read` → 新: `POST /messages/notifications/read/mark`
-
- 旧: `POST /dictionary/dictionaries` → 新: `POST /dictionary/list`
- 旧: `POST /thanks/list-by-user` → 新: `POST /thanks/user/list`
- 旧: `POST /thanks/list-by-torrent` → 新: `POST /thanks/torrent/list`
-
- 旧: `POST /api/songs/lyrics/get` → 新: `POST /music/songs/lyrics/detail`
