## 目标
- 按需求精简控制台设置页面，移除指定配置项与按钮，并将 Passkey 文案改为“重置 Passkey”。
- 变更仅涉及 `src/pages/ControlPage.tsx`，不影响其他页面逻辑。

## 变更范围
- 控制台页面：移除底部操作区的“重置”按钮。
- 个人信息：移除“用户名”“邮箱”的编辑控件与“验证邮箱”按钮，仅保留头像、签名、所在地、简介。
- 网站偏好：移除“显示剧透内容”“自动下载种子文件”“每页显示种子数”。
- 安全设置：移除“会话超时（分钟）”；将“Passkey 登录”区块的标题与按钮文案改为“重置 Passkey”。
- 通知设置：移除“上传成功”。
- 隐私设置：移除“显示活动记录”“删除账户”。

## 具体代码定位与修改
- 删除“重置”按钮：`src/pages/ControlPage.tsx:980-986`。
- 个人信息：
  - 删除“用户名”输入块：`src/pages/ControlPage.tsx:244-257`。
  - 删除“邮箱”输入块及“验证邮箱”按钮：`src/pages/ControlPage.tsx:260-279`。
- 网站偏好：
  - 删除“每页显示种子数”选择器：`src/pages/ControlPage.tsx:397-419`。
  - 删除“自动下载种子文件”开关：`src/pages/ControlPage.tsx:444-461`。
  - 删除“显示剧透内容”开关：`src/pages/ControlPage.tsx:463-480`。
- 安全设置：
  - 删除“会话超时（分钟）”块：`src/pages/ControlPage.tsx:586-612`。
  - Passkey 文案调整：
    - 标题改为“重置 Passkey”：`src/pages/ControlPage.tsx:664`。
    - 按钮文案改为“重置 Passkey”：`src/pages/ControlPage.tsx:673-674`。
- 通知设置：
  - 删除“上传成功”开关块：`src/pages/ControlPage.tsx:787-804`。
- 隐私设置：
  - 删除“显示活动记录”开关块：`src/pages/ControlPage.tsx:872-886`。
  - 删除“删除账户”区块：`src/pages/ControlPage.tsx:947-965`。

## 同步清理
- 状态初始值：为避免无用状态，删除仅用于已移除控件的键：
  - `preferences`: 移除 `torrentsPerPage`、`autoDownload`、`showSpoilers`（若其他页面未引用）。
  - `security`: 移除 `sessionTimeout`（无全局引用）。
  - `notifications`: 移除 `uploadSuccess`（无全局引用）。
  - `privacy`: 移除 `showActivity`（无全局引用）。
  - `profileData.email` 可一并移除；`profileData.username`保留（用于左侧用户卡片与头像 seed）。
- Imports 清理：移除已不再使用的图标与组件引用（如 `Download`、`Eye`、`Upload` 等对应被删除块）。
- UI 分隔线：同步删除对应的 `Separator`，确保视觉结构整洁。

## 原因说明
- 删除指定配置项与按钮是产品需求收敛，避免无效设置与误操作。
- Passkey 改为“重置”更贴近实际动作与用户心智，减少误解。
- 同步移除无用状态与 imports，避免死代码与潜在警告，提高可维护性。

## 验证方案
- 本地启动后逐标签检查：个人信息/网站偏好/安全/通知/隐私，确保无报错、无空白分隔。
- 静态检查：确认 TypeScript/ESLint 无未使用变量与类型错误。
- 交互验证：保存按钮仍可正常显示“设置已保存”反馈。

## 交付
- 完成代码修改并自测通过后提交变更，提供变更点列表与关键行引用以便 CR。