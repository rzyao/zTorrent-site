# PlaylistsPage 页面后端对接说明文件编写计划

## 文档目标
- 输出一份可交付的《PlaylistsPage页面后端对接说明.md》，明确页面所需接口、参数、响应结构与字段补全需求。
- 字段命名尽量与当前前端保持一致，减少改动成本。

## 文档结构
1. 文档目的与页面功能概览
2. 前端数据模型需求（字段与类型）
3. 接口清单与详细规范
   - 列表接口（全部/我的/关注）
   - 关注/取消关注接口
   - 创建片单接口
   - 片单详情接口（供跳转后使用）
   - 浏览量上报接口（可选）
4. 排序与搜索规则映射
5. 鉴权与错误响应规范
6. 需要补全的字段清单
7. 示例请求与响应（JSON）

## 文件落盘位置
- 计划新增到 `c:\project\zTorrent-site\docs\PlaylistsPage页面后端对接说明.md`（若不存在 `docs` 目录，将一并创建）。

## 交付方式
- 经确认后，我将创建上述 Markdown 文件并写入完整内容。

## 后续可选工作（不在本次文档范围）
- 若后端 OpenAPI 已存在部分接口，我会标注需新增或补字段的接口，并在前端引入生成的客户端进行替换模拟数据逻辑。