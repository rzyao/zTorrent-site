# 工单系统功能设计蓝图

## 1. 概述
本方案旨在完善网站的工单反馈系统，采用**页面内二级导航**的设计模式，确保用户在不离开工单模块的情况下完成从提交到追踪的全流程。系统将从单一庞大的组件重构为模块化、易维护的结构。

## 2. 功能架构与导航设计

## 2. 功能架构与导航设计

### 2.1 角色权限与导航结构 (Role-Based Navigation)
系统根据用户角色（**普通用户** vs **管理员/客服**）动态展示不同的二级导航和功能模块。

#### A. 普通用户视图 (User View)
*   **我的工单 (List)**: 查看自己提交的历史工单。
*   **新建工单 (Create)**: 提交新问题。
*   **常见问题 (FAQ)**: 自助解决问题。

#### B. 管理员视图 (Staff View)
*   **工单管理 (All Tickets)**: 查看全站所有工单，支持高级筛选（按用户、IP、未分配等）。
*   **我的待办 (Assigned to Me)**: 仅显示分配给当前管理员处理中的工单。
*   **新建工单**: 管理员代用户创建工单或创建内部任务单。
*   **统计报表 (Stats)**: 查看积压量、处理时效、热点问题分布。

### 2.2 核心功能模块差异
| 模块 | 功能点 | 普通用户权限 | 管理员权限 |
| :--- | :--- | :--- | :--- |
| **工单列表** | 范围 | 仅见自己提交的工单 | 可见所有用户工单 |
| | 搜索 | 仅搜标题/内容 | 支持搜用户名/ID/IP/邮箱 |
| **工单详情** | 属性编辑 | 仅能“关闭” | **修改优先级、分类、分配人、状态** |
| | 消息交互 | 发送普通回复、上传附件 | 发送回复、上传附件、**添加内部备注(用户不可见)** |
| | 用户操作 | 无 | **查看用户画像、禁言、封禁账号** |
| | 快捷回复 | 无 | **使用预设话术(Canned Responses)** |
| **工单提交** | 优先级 | 可选 | 可选，且可设置“内部工单” |

## 3. 详细页面功能说明

### A. 工单列表页 (TicketList)
*   **通用功能**：
    *   顶部状态统计卡片。
    *   筛选工具栏（状态、分类、关键词）。
    *   列表卡片展示基础信息。
*   **管理端特有**：
    *   **分配状态列**：显示当前处理人。
    *   **批量操作**：批量关闭、批量分配。
    *   **高亮显示**：紧急工单高亮、超时未处理工单标红。

### B. 新建工单页 (TicketCreate)
*   保持一致，管理端可额外填写“代表用户ID”字段（若支持代提单）。

### C. 工单详情页 (TicketDetail)
*   **布局调整**：
    *   **普通用户**：左侧对话，右侧基础信息。
    *   **管理员**：三栏布局或右侧增强。
        *   **右侧管理面板**：
            *   **处理人**：下拉选择管理员。
            *   **标签(Tags)**：添加内部管理标签（如“疑难”、“待开发确认”）。
            *   **用户信息**：展示提单人注册时间、上传下载量、历史工单数（以此判断是否恶意投诉）。
*   **回复区增强 (管理端)**：
    *   增加 Tab 切换：【发送给用户】 vs 【**内部备注**】（仅管理团队可见）。
    *   快捷回复按钮：点击插入常用语模板。

### D. 常见问题页 (TicketFAQ) - *建议补充*
*   展示“如何重置密码”、“做种没速度怎么办”等高频问题。
*   提供搜索功能，引导用户自助服务。

## 4. 前端组件拆分方案 (Refactoring Plan)

为了解决当前 `TicketsPage.tsx` 代码臃肿的问题，建议进行如下目录重构：

```text
src/
  pages/
    tickets/
      layout.tsx           # 工单模块主布局（包含二级导航）
      index.tsx            # 入口文件（路由分发）
      TicketListPage.tsx   # 列表页组件
      TicketCreatePage.tsx # 新建页组件
      TicketDetailPage.tsx # 详情页组件
      TicketFAQPage.tsx    # FAQ页组件
  components/
    tickets/
      TicketStatusBadge.tsx # 状态标签组件
      TicketMessageBubble.tsx # 对话气泡组件
      TicketFilterToolbar.tsx # 筛选工具栏
      AttachmentUploader.tsx  # 附件上传组件
```

## 5. 数据流与状态管理
*   **状态共享**：使用 React Context 或 URL Search Params 管理筛选状态，确保从详情页返回列表页时，筛选条件不丢失。
*   **实时性**：在详情页使用轮询 (Polling) 或 WebSocket (如果后端支持) 更新消息，确保用户不用频繁刷新页面即可看到客服回复。

## 6. 后续开发任务清单
1.  [ ] 创建 `src/pages/tickets/` 目录及基础组件结构。
2.  [ ] 提取 `TicketsPage.tsx` 中的 UI 组件到 `src/components/tickets/`。
3.  [ ] 实现二级导航布局 `TicketLayout`。
4.  [ ] 分别实现 List、Create、Detail 子页面逻辑。
5.  [ ] 优化附件上传体验（增加进度条）。
6.  [ ] 增加 FAQ 静态页面（内容待定）。
